const = document.querySelectorAll('');
const = document.querySelectorAll('');
const = document.querySelectorAll('');
const = document.querySelectorAll('');
const = document.querySelectorAll('');

window.addEventListener('load', () =>{

  const TL = gasp.timeline({paused: true});

  TL
  .staggerFrom(, 1, {top: -50, opacity; 0, ease: "power2.out"}, 0.3)

  TL.play();

})
